export interface Comment {
  id?: Number,
  username: string,
  googleAccount?: string,
  ipAddr?: string,
  playerId?: Number,
  clubId?: Number,
  message: string,
  createDate?: string,
  modifiedDate?: string,
}